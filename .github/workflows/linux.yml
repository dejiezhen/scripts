name: linux

on: push

jobs:
  build: 
    runs-on: ubuntu-latest

    steps:
    # Check out major release for job
    - uses: actions/checkout@v2           
    - name: Install Dependencies
    run: ./install-deps.sh

